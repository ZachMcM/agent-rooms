# local | cloud. Resolved once at startup to decide which middleware gets mounted.
AGENT_ROOMS_MODE=local

# Defaults to file:~/.agent-rooms/db.sqlite. Set to a Turso url in cloud mode.
# AGENT_ROOMS_DB_URL=
# AGENT_ROOMS_DB_AUTH_TOKEN=

AGENT_ROOMS_HOST=127.0.0.1
AGENT_ROOMS_PORT=4319
