# local | cloud. Resolved once at startup to decide which middleware gets mounted.
AGENT_COMMS_MODE=local

# Defaults to file:~/.agent-comms/db.sqlite. Set to a Turso url in cloud mode.
# AGENT_COMMS_DB_URL=
# AGENT_COMMS_DB_AUTH_TOKEN=

AGENT_COMMS_HOST=127.0.0.1
AGENT_COMMS_PORT=4319
